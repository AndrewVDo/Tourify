export const clickRegister =    (uid,
                                alias,
                                weight,
                                birthday,
                                nationality,
                                userType) => {  
    console.log(uid)
}